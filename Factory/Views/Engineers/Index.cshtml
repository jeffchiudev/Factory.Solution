@{
    Layout = "_Layout";
}

@using Factory.Models;
@model List<Factory.Models.Engineer>

<h1>Engineer Roster</h1>
<hr/>
@if (@Model.Count == 0)
{
    <h3>Engineer roster is currently empty.</h3>
} 

@foreach (Engineer engineer in Model)
{
    <h4>@Html.ActionLink($"{engineer.EngineerName}", "Details", new { id = engineer.EngineerId })</h4>
}
<hr/>
<p>@Html.ActionLink("Add New Engineer", "Create", null, null, new {@class="btn btn-primary"})</p>
<p>@Html.ActionLink("Back Home", "Index", "Home", null, new {@class="btn btn-primary"})</p>